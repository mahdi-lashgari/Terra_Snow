name: tf-deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  tf-deploy:
    uses: ./.github/workflows/.tf-deploy.yml
    with:
      environment: snowflake-prod
      account_id: ${{ vars.SNOWFLAKE_ACCOUNT }}
    secrets: inherit
