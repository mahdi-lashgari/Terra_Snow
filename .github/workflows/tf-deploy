name: tf-deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  tf-deploy:
    uses: ./.github/workflows/.snowflake.yml
    with:
      environment: snowflake-prod
      account_id: ${{ vars.SNOWFLAKE_ACCOUNT }}
    secrets: inherit
